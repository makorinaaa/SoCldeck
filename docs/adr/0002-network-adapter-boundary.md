# Network adapter boundary

SocialDeck will treat each supported social network as a Network Adapter that exposes capabilities to the app. This keeps the app centered on common operations such as columns, posting, notifications, search, and profile display while allowing each adapter to hide very different mechanics, such as X WebView automation and Bluesky API calls.
